
# Airflow image dependencies
# Keep lean — every package here adds to Docker build time

# PySpark local mode — processes 1.3M rows without hitting RAM limits
pyspark==3.5.1

# Delta Lake on PySpark — ACID + time travel on parquet files
delta-spark==3.1.0

# Data quality contracts}
great-expectations==0.18.19


# Core data libs
pandas==2.2.2
pyarrow==15.0.2

# Utilities
requests==2.32.3

